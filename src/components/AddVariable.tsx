import {
  DetailsList,
  IColumn,
  DefaultButton,
  SelectionMode,
  PrimaryButton,
} from "@fluentui/react";
import * as React from "react";
import { Variable } from "../models/Types";

type AddVariableProps = {};

const AddVariable: React.FunctionComponent<AddVariableProps> =
  ({}: AddVariableProps) => {
    return <></>;
  };

export default AddVariable;
